trigger:
   - main
   
   pool:
      vmImage: "ubuntu-latest'
      # name : 'azure-vmss'
      
   jobs:
   - job: job1
     steps:
        - script: echo Hello, $hostname
   -job: job2
      steps:
        - script: echo Hello, $hostname
   -job: job3
      steps:
        - script: echo Hello, $hostname
   -job: job4
      steps:
        - script: echo Hello, $hostname
        
